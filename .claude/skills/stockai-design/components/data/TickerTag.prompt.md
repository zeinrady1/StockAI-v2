Amber, black-weight stock symbol — the visual anchor for every ticker in the app. Optional buy/sell action label colored by side.

```jsx
<TickerTag symbol="NVDA" />
<TickerTag symbol="PLTR" action="Purchase" />
<TickerTag symbol="TSLA" action="Sale" size="lg" onClick={openChart} />
```
